# Agent LLM 联调结果

- 更新时间：2026-04-19 11:32:24 +08:00
- 更新原因：按用户要求，为 5 个 agent 接入共享 OpenAI-compatible LLM 运行时，并完成一轮真实接口联调验证。
- 影响范围：`apps/orchestrator-service/*`、`logs/manual-swagger-start/orchestrator-service/*`

## 本次实现

1. 在 `orchestrator-service` 内新增共享答案生成层：
   - 统一读取 LLM 运行时环境变量
   - 使用 OpenAI-compatible client
   - 由 5 个 agent 共用同一答案生成入口
   - provider 不可用时回退到原有模板回答

2. 当前运行时已注入模型配置：
   - 模型：`gpt-5.4`
   - OpenAI-compatible base URL：用户提供的测试端点

3. 已完成测试：
   - `tests/test_runtime.py` 新增 LLM 优先与回退测试
   - `20 passed`，未发现运行时回归

## 真实接口联调结果

本次直接调用 `orchestrator-service` 的 `/api/v1/chat/completions`，并分别覆盖：

1. `finance_order_agent`
2. `product_tech_agent`
3. `icp_service_agent`
4. `ops_marketing_agent`
5. `deep_research_agent`

结果：

- 5 条请求全部返回 `HTTP 200`
- 5 个 agent 都成功返回非模板化自然语言回答
- 日志中已记录：
  - `agent answer generator produced response for finance_order_agent using model gpt-5.4`
  - `agent answer generator produced response for product_tech_agent using model gpt-5.4`
  - `agent answer generator produced response for icp_service_agent using model gpt-5.4`
  - `agent answer generator produced response for ops_marketing_agent using model gpt-5.4`
  - `agent answer generator produced response for deep_research_agent using model gpt-5.4`

## 当前结论

- 现在可以确认：
  - 5 个 agent 已经能通过共享答案生成层使用同一个 LLM key
  - 当前并不是只有模板回答
  - 若后续 provider 不可用，系统仍能回退到原有模板逻辑，不会直接把主链路打挂

## 已知限制

- 目前是“共享答案生成层 + 现有规则路由/工具编排”的形态，不是完整 LangGraph 主编排。
- 目前主要由 `orchestrator-service` 统一调用模型，而不是每个 agent 独立维护一套 provider 配置。
