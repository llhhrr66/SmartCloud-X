# LLM 运行时配置记录

- 更新时间：2026-04-19 02:40:53 +08:00
- 更新原因：按用户要求，为当前本地联调进程补充 OpenAI-compatible LLM 运行时环境变量，用于后续接入测试。
- 影响范围：本地手工启动的 `orchestrator-service` 进程环境、`logs/manual-swagger-start/orchestrator-service/`

## 本次动作

1. 已为本地重启后的 `orchestrator-service` 运行时注入一组 LLM 相关环境变量：
   - 标准命名：
     - `OPENAI_API_KEY`
     - `OPENAI_BASE_URL`
     - `OPENAI_MODEL`
   - 项目预留命名：
     - `SMARTCLOUD_LLM_API_KEY`
     - `SMARTCLOUD_LLM_BASE_URL`
     - `SMARTCLOUD_LLM_MODEL`

2. 出于安全原因，本记录不落地保存密钥明文。

## 当前限制

1. 当前仓库中尚未发现真正实例化 OpenAI / OpenAI-compatible client 的业务主链路代码。
2. `orchestrator-service` 目前仍是规则路由 + 模板回答 baseline，不会仅因注入 key 就自动切换到真实 LLM 回答。
3. 因此，本次配置属于“运行时预配置”，不是“模型接入已生效”。

## 下一步含义

- 后续若要让 `gpt-5.4` 真正参与编排/回答，需要继续补：
  - provider 配置读取
  - client 初始化
  - 调用点接入
  - 错误处理 / 超时 / 回退策略
