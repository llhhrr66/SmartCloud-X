# 网关联调测试结果 - 2026-04-19

- 更新时间：2026-04-19 12:19:46 +08:00
- 记录原因：对本地启动的 `gateway-service` 及其下游服务进行真实 HTTP 联调，确认当前项目到底哪些链路已经能跑，哪些只是占位，哪些存在配置/代码缺陷。

## 1. 本轮涉及的关键变更

### 1.1 代码修复

- 已修复 `research-service` 列表接口在混合 `offset-naive` / `offset-aware` 时间排序时抛出的 `TypeError`
- 修复文件：
  - `D:\1HTML5\实习项目\SmartCloud-X\apps\research-service\app\store.py`
- 新增回归测试：
  - `D:\1HTML5\实习项目\SmartCloud-X\apps\research-service\tests\test_research_api.py`
- 回归结果：
  - `uv run --with-requirements requirements.txt --with httpx pytest tests/test_research_api.py -q`
  - 结果：`21 passed`

### 1.2 运行时修正

- 发现 `research-service` 在严格鉴权模式下还存在两个运行时问题：
  1. JWT 密钥与当前 `auth-user-service` 实际签发 token 的密钥不一致
  2. 服务内 `urllib` 访问 `http://127.0.0.1:8001/internal/v1/auth/validate-token` 时受到系统代理影响，返回 `502`
- 当前已通过运行时环境修正并重启 `research-service`：
  - `SMARTCLOUD_JWT_SECRET=smartcloud-x-dev-secret`
  - `NO_PROXY=127.0.0.1,localhost`
  - `no_proxy=127.0.0.1,localhost`
  - `HTTP_PROXY=`
  - `HTTPS_PROXY=`

### 1.3 启动脚本修正

- 已修正 `D:\1HTML5\实习项目\SmartCloud-X\logs\supervisor-gateway\run-*.ps1` 中与本轮问题直接相关的配置：
  - 所有脚本中的 `SMARTCLOUD_JWT_SECRET` 从 `replace-me` 改为 `smartcloud-x-dev-secret`
  - `run-research-service.ps1` 额外加入 `NO_PROXY/no_proxy` 与空代理环境变量，避免本地内部鉴权请求被代理

## 2. 当前本地运行状态

- 当前本地服务仍在运行，Swagger 可直接打开：
  - `gateway-service` `http://127.0.0.1:8000/docs`
  - `auth-user-service` `http://127.0.0.1:8001/docs`
  - `marketing-service` `http://127.0.0.1:8002/docs`
  - `research-service` `http://127.0.0.1:8003/docs`
  - `orchestrator-service` `http://127.0.0.1:8010/docs`
  - `tool-hub-service` `http://127.0.0.1:8020/docs`
  - `business-tools-service` `http://127.0.0.1:8030/docs`
  - `knowledge-service` `http://127.0.0.1:8031/docs`
  - `rag-service` `http://127.0.0.1:8040/docs`

## 3. 最终联调结论

### 3.1 已验证通过的链路

- 用户登录：`200`
- 管理员登录：`200`
  - 注意：admin 登录必须带 `captcha_token`
- 管理员仪表盘：`200`
- 知识库总览：`200`
  - 当前总览数据：`sources=13`、`documents=14`
- RAG 回答：`200`
  - 当前返回引用数：`3`
- 账单汇总：`200`
  - 当前 demo 用户账单结果为 `total_amount=0.00`
- 工单列表：`200`
  - 当前总数：`1`
- ICP 列表：`200`
  - 当前总数：`1`
- 营销活动列表：`200`
  - 当前总数：`2`
- 海报任务列表：`200`
  - 当前总数：`3`
- 研究任务列表：`200`
  - 当前总数：`4`
- 5 个 Agent 网关聊天：
  - `finance_order_agent`：`200`，`status=success`
  - `product_tech_agent`：`200`，`status=success`
  - `icp_service_agent`：`200`，`status=need_user_input`
  - `ops_marketing_agent`：`200`，`status=success`
  - `deep_research_agent`：`200`，`status=success`

### 3.2 已确认的使用前提

- 网关聊天 `/api/v1/chat/completions` 不是“裸调即用”
- 需要先创建会话 `/api/v1/chat/sessions`
- 管理端知识库与 RAG 入口是管理员鉴权，不是普通用户接口：
  - `/api/knowledge/v1/*`
  - `/api/rag/v1/*`

### 3.3 当前仍然偏弱或不完整的地方

- 账单/订单链路虽然接口能通，但 demo 用户当前账单是空的，订单查询示例号返回 `404`
- 这说明“接口存在”和“真实业务数据已打通”不是一回事，至少订单/账单对普通测试用户仍偏占位
- `gateway-service` 的本地工作区缓存仍然承担了部分用户态数据承接职责，例如：
  - 订单快照
  - 工单
  - ICP 申请
  - 上传文件
  - 引用缓存
- 对应实现文件：
  - `D:\1HTML5\实习项目\SmartCloud-X\apps\gateway-service\app\services\store.py`
- 也就是说，当前项目不是“废了”，但确实存在“一部分真实落库，一部分网关本地兼容态”的混合架构

## 4. 对你接下来测试最有用的结论

- 现在可以优先从这些 Swagger 开始点：
  - `http://127.0.0.1:8000/docs`
  - 先测用户链路：
    - `/api/v1/auth/login`
    - `/api/v1/tickets`
    - `/api/v1/icp/applications`
    - `/api/v1/marketing/campaigns`
    - `/api/v1/marketing/posters`
    - `/api/v1/research/tasks`
  - 再测管理链路：
    - `/api/v1/admin/auth/login`
    - `/api/knowledge/v1/overview`
    - `/api/knowledge/v1/search`
    - `/api/rag/v1/answer`
  - 聊天链路请按这个顺序：
    1. `/api/v1/chat/sessions`
    2. `/api/v1/chat/completions`

## 5. 当前状态判断

- 这个项目不能判定为“已经废了”
- 但也不能判定为“已经做完”
- 更准确的判断是：
  - 聊天 Agent、营销、研究、知识库/RAG、工单、ICP 已经有可测主链路
  - 账单/订单更像半成品或 demo 态
  - 网关兼容层承担了不少本地状态缓存，说明系统还处在“联调过渡架构”，不是最终形态

## 6. 当前运行状态补记

- 更新时间：2026-04-19 13:22:15 +08:00
- 变更原因：用户要求先暂停本地联调服务运行
- 已停止的本地端口：
  - `8000`
  - `8001`
  - `8002`
  - `8003`
  - `8010`
  - `8020`
  - `8030`
  - `8031`
  - `8040`
- 当前结果：以上端口均已释放，本地后端服务已暂停
